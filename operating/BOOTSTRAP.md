# KEFAYAT Ω — Zero-Burden Resume Bootstrap

For every new executor/session:

1. Read `governance/verified-baseline-control-plane.md`.
2. Read and validate `state/verified-state.json`.
3. Read `baselines/golden-render/contract.json` before generating student-facing mathematics.
4. Treat historical versions as evidence unless the active authority explicitly promotes them.
5. Re-check repository/CI/runtime evidence; do not trust the last chat message as state.
6. Resume from the last evidence-backed baseline.
7. Preserve every protected invariant and passing regression.
8. Execute the highest-value safe remaining action without transferring technical work to the user.
9. After any change: verify → adversarially attack → repair → reverify → regression → record evidence.
10. Promote a candidate only after all applicable blocking gates pass on the exact final artifact.

Canonical math sanity check before release:
`ENGINE □ = ٣ + ٤` must lead to `STUDENT-EYE ٤ + ٣ = □`.

If platform-specific behavior cannot be observed, record `NOT_PROVEN`; do not rewrite the protected rule to fit an unverified assumption.
